# Lab 1 Starter

Implement the `TODO` markers in `lab01/models.py` and `lab01/providers.py`.

Run the target tests from this directory:

```powershell
..\..\..\.venv\Scripts\python.exe -m pytest -q
```

The initial starter is expected to fail. Completion means all tests pass and recorded inputs never call the OpenAI API.
