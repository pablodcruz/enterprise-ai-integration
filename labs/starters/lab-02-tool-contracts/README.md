# Lab 2 Starter

Implement the two narrow tool handlers in `lab02/tools.py`. The proposal tool must never append a comment or simulate a completed write.

```powershell
..\..\..\.venv\Scripts\python.exe -m pytest -q
```
