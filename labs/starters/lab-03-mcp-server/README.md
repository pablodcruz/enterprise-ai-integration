# Lab 3 Starter — Build an MCP Server

This directory contains the learner starter for [Lab 3 — Build an MCP Server](../../lab-03-build-an-mcp-server.md).

Start with the main lab guide. It explains the architecture, tool contracts, relevant SDK patterns, milestones, expected outputs, Inspector workflow, troubleshooting, and reflection questions.

The deterministic incident service and fixture are complete. Your work is bounded to the `TODO` sections in `lab03/server.py` and `Dockerfile`.

From this directory, run the intentional baseline failures with:

Git Bash:

```bash
../../../.venv/Scripts/python.exe -m pytest -q
```

PowerShell:

```powershell
..\..\..\.venv\Scripts\python.exe -m pytest -q
```

Do not inspect `solutions/labs/lab-03-mcp-server/` until you have completed the lab or recorded where you became blocked.
