# Lab 4 Starter

Implement direct protocol discovery and invocation before adding the model. Then build a remote-MCP tool configuration that imports only the required tools and keeps approvals enabled.

```powershell
..\..\..\.venv\Scripts\python.exe -m pytest -q
```
