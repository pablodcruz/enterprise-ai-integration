# Lab 2 Reference Solution

The solution uses Pydantic contracts at the tool boundary, a fixture-backed store, bounded search, controlled domain errors, and a proposal tool that never mutates the incident.

```powershell
..\..\..\.venv\Scripts\python.exe -m pytest -q
..\..\..\.venv\Scripts\python.exe -m lab02.cli
```
